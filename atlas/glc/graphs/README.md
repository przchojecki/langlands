# Graph exports

- `claim_dependency_edges.csv`: edge list (src → dst) with source/target metadata
- `claim_dependency_edges.jsonl`: same as JSONL
- `graph_stats.json`: summary counts
- `top20_in_degree_subgraph.svg`: a small visual subgraph around the 20 most referenced claims
